from openerp.osv import osv
from openerp.osv import fields
from openerp.tools.translate import _
import time

class table_sheet_info(osv.osv):

    _inherit = 'hr.contract'

    _columns = {

                
                'instead_of_performing' : fields.float(
                'Instead of performing',
                size=100,
                required=True,
                    ),

                'instead_of_excellence' : fields.float(
                    'Instead of Excellence',
                    size=100,
                    required=True,
                ),


                'rather_than_the_nature_of_the_work' : fields.float(
                    'Rather than the nature of the work',
                    size=100,
                    required=True,

                ),

                'instead_of_scarcity' : fields.float(
                    'Instead of scarcity',
                    size=100,
                    required=True,

                ),

                'representation_allowance' : fields.float(
                    'Representation allowance',
                    size=100,
                    required=True,

                ),
                'instead_of_establishing' : fields.float(
                    'Representation allowance',
                    size=100,
                    required=True,

                ),



                'instead_phone' : fields.float(
                    'Instead Phone',
                    size=100,
                    required=True,

                ),

                'other_allowances' : fields.float(
                    'Other allowances',
                    size=100,
                    required=True,

                ),
                'incentives_for_projects' : fields.float(
                    'Incentives for projects',
                    size=100,
                    required=True,

                ),
                'the_amount,_which_is_deducted_from_the_basic_insurance' : fields.float(
                    'The_amount,_which_is_deducted_from_the_basic_insurance',
                    size=100,
                    required=True,

                ),
                'the_amount,_which_is_deducted_from_the_changing_insurance' : fields.float(
                    'The amount, which is educted from the changing insurance',
                    size=100,
                    required=True,

                ),

                'guaranteed_bonuses' : fields.float(
                    'Guaranteed bonuses',
                    size=100,
                    required=True,

                ),
        }

table_sheet_info()
